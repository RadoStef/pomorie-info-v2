import { SectionHeaderWrapper } from "../UI/SectionHeaderWrapper";

export const CityObjectHeader = ({photographersDescriptions}) => {
  return (
    <SectionHeaderWrapper>
      <p>{photographersDescriptions}</p>
    </SectionHeaderWrapper>
  )
};
